const paths = require('./paths')

const alias = {
  src: paths.SRC,
  app: paths.APP,
  config: paths.CONFIG,
  core: paths.CORE,
  pages: paths.PAGES,
  root: paths.ROOT,
  public: paths.PUBLIC,
  '@assets': paths.ASSETS,
  '@cosmos': paths.COSMOS,
  '@cosmos-web': paths.COSMOS_WEB,
  '@space-lib-events': paths.SPACE_LIB_EVENTS
}

module.exports = alias